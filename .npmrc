# pnpm outdated で特定のパッケージを無視する設定
# Node.js 22を使用しているため、@types/node は22系を使用
update-ignore-updates=@types/node
# パッケージを強制的にホイストする
shamefully-hoist=true
# package.jsonのenginesフィールドに記載されたNode.js/pnpmバージョンを厳密にチェック
# 指定されたバージョン以外ではインストールを拒否する
engine-strict=true
