{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["BUILD_CONFIGURATION"],
      "outputs": ["dist/**"]
    },
    "test:coverage": {
      "outputs": ["coverage/**"]
    }
  }
}
